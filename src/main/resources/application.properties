spring.application.name=soft-outlet

server.port=9191

spring.datasource.url=jdbc:postgresql://localhost:5433/soft_outlet
spring.datasource.username=postgres
spring.datasource.password=1qw23er4

#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=create

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true

spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# Add apiKey to application.properties
#rapidapp.postgres.enabled=true
#rapidapp.postgres.apiKey=${RAPIDAPP_API_KEY}


api.prefix=/api/v1